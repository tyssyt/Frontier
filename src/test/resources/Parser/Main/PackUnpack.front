import IO;
import Strings;
import List;

export class Main:

static f() -> char, char, char {
    return IO.getChar(),IO.getChar(),IO.getChar();
}

static g() -> int32, int32 {
    return 66,99;
}

export static main () {
    (a:, b:, c:), e:int64 = f(), 8888;
    IO.print(a);
    IO.print(b);
    IO.print(c);
    printInt(e);
    IO.printLine("");

    a,b = b,a;
    IO.print(a);
    IO.print(b);
    IO.print(c);
    IO.printLine("");

    a, b, c, d: = b, c, a+b, a+2*b+c;
    printInt(a);
    printInt(b);
    printInt(c);
    printInt(d);
    IO.printLine("");

    arr :[int32] = [int32, 5];
    arr[2] = -42;
    arr2 := arr;
    arr, arr[2], f:int32 = [int32, 3], 42, 0; //wait, thats illegal
    printInt(arr[2]);
    printInt(arr2[2]);
    IO.printLine("");

    l := List<([char], int64)>.create();

    l.add(("string", e));
    l.add(("g2esports.com/shop", 42));
    qwe:[char], rtz:int64 = l[0];
    qwe2:[char], rtz2:int64 = l[1];
    printInt(rtz);
    IO.printLine(qwe);
    printInt(rtz2);
    IO.printLine(qwe2);
}

static printInt(l: int64) {
    IO.print(Strings.intToString(l));
}