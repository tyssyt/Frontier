import IO;
import Type;

export class Main:

export static main() {
    for t : Type.allTypes {
        IO.printLine(t.name);
        for f : t.fields {
            IO.print("   ");
            IO.print(f.name);
            IO.print(" : ");
            IO.printLine(f.type.name);
        }
    }
}