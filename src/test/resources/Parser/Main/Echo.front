import IO;

export class HelloWorld {
    export static void main() {
        char c := IO.getChar();
        while(c =!*= '\n') {
            IO.print(c);
            c = IO.getChar();
        }
    }
}