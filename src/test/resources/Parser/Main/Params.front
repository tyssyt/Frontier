import IO;
import List;

export class Main:

static getChar(c: char = IO.getChar()) -> char {
    return c;
}

do(y: int32, x: int32) -> int32 {
    return x+y;
}

/*
static crazy <V,R> ((V->R) f := \t -> t.do(x=1, y=100), V v) -> R {
    return f(v);
}
*/

export static main () {
    fun : (Main -> int32) = \t -> t.do(x=15, y=5);
    IO.printInt(fun(new Main()));
    IO.print(getChar("@"));
    IO.print(getChar("#"));
    IO.print(getChar());
    IO.print(getChar("\n"));
/*
    TODO do sth with crazy
*/
}